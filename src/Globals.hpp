#pragma once
#include <GLFW/glfw3.h>
#include <glm/glm.hpp>
#include <string>

// Global variables
inline bool vsyncEnabled = true;
inline GLfloat r = 1.0f, g = 0.0f, b = 0.0f;
inline int windowWidth = 800;
inline int windowHeight = 600;